const newUser = (id, name, email) => {
  return {
    // id: id,
    // name: name,
    // email: email,
    id,
    name,
    email,   
  }
}
console.log(newUser(54, 'Isabella', 'isabella@email.com'));